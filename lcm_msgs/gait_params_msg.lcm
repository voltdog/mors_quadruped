package mors_msgs;

struct gait_params_msg
{
    boolean standing;

    float gait_type[4]; // legs phase offsets eg: [0, 0.25, 0.5, 0.75] for walk

    float t_sw; // swing phase duration
    float t_st; // stance phse duration

    float stride_height;
}